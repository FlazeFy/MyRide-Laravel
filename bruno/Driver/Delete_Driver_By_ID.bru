meta {
  name: Hard Delete Driver By ID
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/driver/destroy/:id
  body: none
  auth: bearer
}

params:path {
  id: e1288783-a5d4-1c4c-2cd6-0e92f7cc3bf9
}

auth:bearer {
  token: {{token}}
}

docs {
  # Hard Delete Driver By ID

  This request is used to permanently delete a driver entry based on the provided `ID`. This request interacts with the MySQL database, has a protected routes, and audited activity (history).

  ## Path Parameters

  | Name | Type   | Required | Description | Example                               |
  |------|--------|----------|--------------|------------------------------------------|
  | id   | string | yes      | Driver ID   | e1288783-a5d4-1c4c-2cd6-0e92f7cc3bf9   |

  ## Responses

  **200 — driver permanently deleted**
```json
  {
    "status": "success",
    "message": "driver permanently deleted"
  }
```

  **401 — protected route need to include sign in token as authorization bearer**
```json
  {
    "status": "failed",
    "message": "you need to include the authorization token from login"
  }
```

  **404 — driver failed to permanently deleted**
```json
  {
    "status": "failed",
    "message": "driver not found"
  }
```

  **500 — Internal Server Error**
```json
  {
    "status": "error",
    "message": "something wrong. please contact admin"
  }
```
}