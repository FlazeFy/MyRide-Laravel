meta {
  name: Post Update Vehicle Image By ID
  type: http
  seq: 11
}

post {
  url: {{baseUrl}}/vehicle/image/:id
  body: multipartForm
  auth: bearer
}

params:path {
  id: e1288783-a5d4-1c4c-2cd6-0e92f7cc3bf9
}

body:multipart-form {
  vehicle_img_url: @file()
}

auth:bearer {
  token: {{token}}
}

docs {
  # Post Update Vehicle Image By ID

  This request is used to update vehicle image by given vehicle's `ID`. And the updated field is `vehicle_img_url`. This request interacts with the MySQL database, firebase storage, has a protected routes, and audited activity (history).

  ## Path Parameters

  | Name | Type   | Required | Description | Example                               |
  |------|--------|----------|--------------|------------------------------------------|
  | id   | string | yes      | Vehicle ID  | e1288783-a5d4-1c4c-2cd6-0e92f7cc3bf9   |

  ## Responses

  **201 — Vehicle image update successfully**
```json
  {
    "status": "success",
    "message": "vehicle updated"
  }
```

  **400 — Validation failed**
```json
  {
    "status": "failed",
    "message": "vehicle image is a required field"
  }
```

  **404 — vehicle failed to fetched**
```json
  {
    "status": "failed",
    "message": "vehicle not found"
  }
```

  **500 — Internal Server Error**
```json
  {
    "status": "error",
    "message": "something wrong. please contact admin"
  }
```
}