meta {
  name: Get Total Service Per Year
  type: http
  seq: 8
}

get {
  url: {{baseUrl}}/stats/total/service/monthly/:context/:year
  body: none
  auth: bearer
}

params:path {
  context: total_item
  year: 2026
}

auth:bearer {
  token: {{token}}
}

docs {
  # Get Total Service Per Year
  
  This request is used to get total service per month by given `year` and `context`. This request is using MySql database.
  
  ## Path Parameters
  
  | Name    | Type    | Required | Description                                  | Example    |
  |---------|---------|----------|-------------------------------------------------|------------|
  | context | string  | yes      | Context can be `total_item` or `total_price`   | total_item |
  | year    | integer | yes      | Service created year                            | 2024       |
  
  ## Responses
  
  **200 — stats fetched**
  ```json
  {
    "status": "success",
    "message": "stats fetched",
    "data": [
      { "context": "Jan", "total": 3 }
    ]
  }
  ```
  
  **401 — protected route need to include sign in token as authorization bearer**
  ```json
  {
    "status": "failed",
    "message": "you need to include the authorization token from login"
  }
  ```
  
  **404 — stats failed to fetched**
  ```json
  {
    "status": "failed",
    "message": "stats not found"
  }
  ```
  
  **500 — Internal Server Error**
  ```json
  {
    "status": "error",
    "message": "something wrong. please contact admin"
  }
  ```
}
